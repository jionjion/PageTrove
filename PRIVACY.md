# 拾页 (PageTrove) 隐私政策

**最后更新日期：2026-07-21**

拾页 (PageTrove) 是一款浏览器扩展，帮助用户剪藏网页内容并借助 AI 进行智能标注。本隐私政策说明扩展收集哪些数据、如何使用以及如何存储。

## 数据收集

### 我们收集什么

拾页仅在**用户主动操作**时（如点击"读取页面"或"分析"）才会采集数据。扩展**不会**在后台被动监控或追踪你的浏览行为。

当你剪藏某个页面时，扩展会提取以下公开可见的页面元数据：

- 页面 URL（若存在则包含 canonical URL）
- 页面标题和描述（来自 meta 标签）
- 页面正文内容（提取自 `<article>`、`<main>` 或 `<body>`）
- 作者和发布日期（来自 meta 标签）
- 页面语言
- 网站图标 (Favicon) URL
- 用户选中的文本（仅在有选中内容且设置中已启用时）

使用"元素选取"功能时，仅提取你点击选中的元素的可见文本。

### 我们不收集什么

- 浏览历史（仅处理你主动选择剪藏的页面）
- 表单输入、密码或登录凭证
- Cookie 或会话数据
- 设备指纹或硬件标识符
- IP 地址、位置信息或联系方式
- 摄像头或麦克风数据
- 任何你未主动选择剪藏的页面数据

## 数据存储

所有数据均通过浏览器的 `browser.storage.local` API **存储在你的本地设备上**，包括：

- **剪藏数据**：URL、标题、摘要、标签、笔记和提取的文本
- **聊天记录**：与 AI 助手的对话历史
- **扩展设置**：你选择的 AI 服务商、API Key、模型和偏好配置

数据不会同步至任何云服务，也不会发送给扩展开发者。你的数据完全保留在本机，直到你主动删除或清除扩展存储。

## 向第三方传输的数据

拾页仅连接**一个**外部服务：你在设置中自行配置的 AI API 服务商（如 DeepSeek、OpenAI、Kimi、智谱 GLM、通义千问，或自定义端点）。

当你点击"分析"或使用聊天功能时，以下数据会发送至你选择的 AI 服务商：

- 页面标题、URL 和描述
- 页面正文内容
- 你选中的文本（如有）
- 你的笔记或聊天消息
- 聊天历史（最近 12 条消息，用于对话上下文）

这些数据**仅在你主动请求时发送**，扩展不会在后台向 AI 服务商发送任何数据。

**重要提示**：各 AI 服务商有各自的隐私政策和数据保留规则，请查阅你所选服务商的相关条款。

## API Key 处理

你的 API Key 存储在本地 `browser.storage.local` 中，仅用于向你选择的 AI 服务商进行身份认证。API Key 不会发送给扩展开发者或任何其他第三方。

## 分析与追踪

拾页**不包含**任何形式的数据分析、遥测、崩溃上报或用户追踪代码。扩展发起的唯一网络请求是发往你自行配置的 AI API 端点。

## 数据导出与删除

- 你可以随时将所有剪藏数据**导出**为 JSON 文件。
- 你可以在扩展内**删除**单个剪藏或聊天记录。
- 你可以通过浏览器设置清除扩展存储来移除所有数据。

## URL 清洗

在存储 URL 之前，扩展会自动去除常见的追踪参数（如 `utm_source`、`utm_medium`、`fbclid`、`gclid`），以减少不必要的数据并保护你的隐私。

## 权限说明

| 权限 | 用途 |
|---|---|
| `activeTab` | 用户点击扩展图标时获取当前标签页的基本信息（标题、URL） |
| `scripting` | 向当前页面注入只读脚本，提取公开可见的文本和元数据 |
| `storage` | 在本地保存剪藏数据、聊天记录和扩展设置 |
| `tabs` | 查询当前活动标签页以在侧边栏显示页面信息，并为脚本注入提供目标 tabId |
| `sidePanel` | 将扩展主界面呈现为浏览器侧边栏 |
| `<all_urls>` | 允许对用户选择剪藏的任意网站注入采集脚本（侧边栏内的按钮点击不会触发 `activeTab` 授权，因此需要更广泛的主机权限） |
| `https://api.deepseek.com/*` | 向默认 AI API 端点发送请求 |

## 政策变更

如本隐私政策有更新，将在文档顶部标注新的"最后更新日期"。

## 联系方式

如对本隐私政策有疑问，请在项目的 [GitHub 仓库](https://github.com/jionjion/PageTrove) 提交 Issue。
