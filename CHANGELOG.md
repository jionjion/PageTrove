# Changelog

## v1.3

### ✨ 新增

- 本地数据存储重构：收藏和聊天迁移至浏览器 IndexedDB，详情与列表索引在同一事务中写入，避免数据不一致
- 设置页导入收藏后，已打开的侧边栏会自动刷新列表
- 新增 `unlimitedStorage` 权限，防止磁盘紧张时收藏和聊天数据被浏览器回收
- 导入收藏时对文件内重复 URL 去重，只保留第一条
- 新增个人中心，实时统计使用量

### ⚠️ 重要提示

> 本版本重构本地数据存储。内测数据不会自动迁移；如需保留收藏，请在升级前导出 JSON，并在升级后重新导入。旧聊天历史不会保留。

## v1.2

### ✨ 新增

- 右键选中文字后可「向拾页提问」，选中内容以引用卡片进入侧边栏，需手动发送
- 新增「探究」视图，可勾选当前窗口 2–5 个网页进行多来源问答，回答带 `[S1]` 来源标注
- 收藏搜索覆盖正文、备注和选中文字，正文命中显示上下文片段
- 收藏列表新增选择模式，可对 2–5 条收藏发起限定范围探究问答
- 收藏可导出为 Obsidian 兼容 Markdown：单条 `.md`、多条或全部为带索引的 `.zip`
- 新增 `contextMenus` 权限，仅用于选中文字后显示右键菜单

## v1.1

### ✨ 新增

- 支持远程 Streamable HTTP 与 SSE MCP Server，可按服务和工具启用或停用
- 支持按模型配置图片输入与工具调用能力
- 支持手动框选页面区域进行截图问答，截图不写入历史记录
- 元素定位可识别图片、图表、Canvas、SVG 和 CSS 背景，并按需附加区域截图


## v1.0

### ✨ 新增

- 接入 AI 对话，可以在侧边栏和 AI 聊天
- 支持查看聊天历史记录
- 可以选取网页中的某个元素进行采集
- 新增设置页面，可以自己填 API Key
- 采集过的内容可以在列表中查看和管理
- 提交标签后自动打包，方便下载安装

### 💄 变更

- 界面换用 Ant Design，风格更统一
- 重新整理了项目代码目录
- 调整了侧边栏的样式外观
- 换了新图标
